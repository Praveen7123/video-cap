# Clean, install-reliable dependency set for deployment (no URL wheels / private pkgs).
fastapi==0.110.1
uvicorn==0.25.0
python-multipart==0.0.32
python-dotenv==1.2.2
email-validator==2.3.0
bcrypt==5.0.0
PyJWT==2.13.0
httpx==0.27.2
supabase==2.9.1
motor==3.3.1
pymongo==4.6.3
# Real local transcription. Needs ~1GB RAM — drop this line for a tiny instance
# (captions then fall back to placeholder text unless you use an API backend).
faster-whisper==1.2.0
